name: permissions_handler
description: A brick to create a permissions handler repository with iOS/Android setup tutorial

version: 1.0.0+1

environment:
  mason: ^0.1.0

vars:
  project_name:
    default: myapp
    type: string
    description: The project name
    prompt: What is the project name?
